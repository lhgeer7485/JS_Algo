function solution(my_string) {
    var answer = '';

    // answer = Array.from(my_string).reverse();

    // answer = '.'.repeat(10).split('.');

    answer = Array(10).fill('')

    return answer;
}

console.log(solution('jaron'));